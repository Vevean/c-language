#include<stdio.h>
int main()
{
	int x,y,z;
	int n,count=0;
	scanf("%d",&n);
	for(x=1;x<30;x++)
	{
		for(y=1;y<75;y++)
		{
			for(z=1;z<150;z++)
			{
				if(x+y+z==100&&5*x+2*y+z==150)
				{
					count++;
					if(count<=n)
					printf("%d %d %d\n",x,y,z);
				}
			}
		}
	}
 } 
