#include<stdio.h>
int main()
{
	int N,number,i;
	int j=0;
	int n=0;
	scanf("%d",&N);
	for(i=1;i<=N;i++){
		scanf("%d",&number);
		if(number%2==0){
			j++;
		}else{
			n++;
		}
	}
	printf("%d %d",n,j);
	return 0;
}
